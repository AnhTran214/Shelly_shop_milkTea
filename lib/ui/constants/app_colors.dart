// Tạo các biến màu để sử dụng lại được nhiều lần

import 'package:flutter/material.dart';

const Color bgColorItems = Color(0xFF6F4E37);
const Color bgColor = Color(0xFFffffff);
const Color textColor = Color.fromARGB(255, 0, 0, 0);
const Color textColorDisable = Colors.grey;
